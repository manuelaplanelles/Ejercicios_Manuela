package org.example;
import java.util.Scanner;
public class CosasNumeritos {
    public static void main() {
        int num1=124;
        int num2=212;

        String num_sring=Integer.toString(num);
        String num_izquierda=num_sring.substring(0,1);
        int num_izquierda_int=Integer.porseInt(num_izquierda);
        System.out.println(num_izquierda_int*100);
    }
}